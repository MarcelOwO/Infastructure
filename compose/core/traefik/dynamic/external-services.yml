http:
  serversTransports:
    insecure-skipverify:    
      insecureSkipVerify: true

  routers:
    images:
      rule: Host(`images.marcel-sigl.dev`)
      entryPoints: [web]
      service: images-svc

  services:
    images-svc:
      loadBalancer:
        servers:
          - url: http://10.0.0.11:2283   

